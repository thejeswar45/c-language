#include <stdio.h>

int main() 
{
   int d,m,year;
   printf("Enter date:");
   scanf("%d",&d);
   printf("Enter month:");
   scanf("%d",&m);
   printf("enter year");
   scanf("%d",&year);
   
   if((d>=1 && d<=31) && (m>=1 && m<=12) && (year>0))
   {
       if(year%400==0)
       {
           printf("%d is a leap year",year);
       }
       else if(year%100==0)
       {
           printf("%d is not aleep year",year);
       }
       else if(year%4==0)
       {
           printf("%d is a leap year",year);
       }
       else
       printf("invalid");
     return 0;  
   }
}
